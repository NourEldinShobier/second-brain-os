/** Resolved workspace locations (implementation in later tasks). */
export interface WorkspacePaths {
  readonly root: string;
  readonly configRelativePath: string;
  readonly dbRelativePath: string;
}
