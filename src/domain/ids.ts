/** Stable primary key in SQLite and frontmatter; opaque string in v1. */
export type EntityId = string;

/** URL- and filename-safe identifier; may appear in paths. */
export type EntitySlug = string;
